import 'package:flutter/material.dart';

const primaryBgColor = Color.fromARGB(255, 255, 255, 255);
// final primaryBgColor = Colors.grey[200];

const primaryThemeColor = Color(0xFF4E60FF);
const secondaryThemeColor = Color(0xFF898EBC);

const textPrimaryColor = Color(0xFF061737);
const textSecondaryColor = Color.fromARGB(255, 255, 255, 255);

const xxLargeTextFontSize =
    26; // For really important texts like product name and the flash screen
const xLargeTextFontSize =
    24; // Section Headers, big status update texts and more
const largeTextFontSize = 22; // Product Header and more
const semiLargeTextFontSize = 20; // For Pricing related text
const mediumBigTextFontSize = 18;
const bodyTextFontSize = 16;
const miniTextFontSize = 14;
